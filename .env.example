### Configuration example for wa-iot-bot
# Copy this file to `.env` and fill the values. Do NOT commit real secrets to git.

# MQTT configuration (required)
MQTT_URL=mqtt://your-mqtt-broker:1883
MQTT_USERNAME=
MQTT_PASSWORD=

# App passphrase used for text commands (example: "1234 lampu1 on")
PASS_PHRASE=1234

# Use Meta Cloud API for interactive messages (buttons/list)
# Set to 1 to enable sending interactive messages through WhatsApp Cloud API
USE_META_API=0

# Meta (WhatsApp Cloud API) credentials — fill if USE_META_API=1
META_API_TOKEN=YOUR_META_API_BEARER_TOKEN_HERE
META_PHONE_NUMBER_ID=YOUR_META_PHONE_NUMBER_ID_HERE
# optional, e.g. v17.0 or v22.0
META_API_VERSION=v17.0

# Optional: developer/debug toggles
LOG_LEVEL=debug
# Konfigurasi MQTT Broker (di VPS kedua)
MQTT_URL=mqtt://IP_BROKER_ANDA:1883
MQTT_USERNAME=username_mqtt_jika_ada
MQTT_PASSWORD=password_mqtt_jika_ada

# MQTT Topics (Pastikan sama dengan di ESP32)
MQTT_TOPIC_LAMP=esp32/lamp
MQTT_TOPIC_FAN=esp32/fan
# Tambahkan topic lain jika perlu

# (Opsional) Passphrase untuk alternatif kontrol via teks
# Contoh: ketik "1234 lampu on"
PASS_PHRASE=1234